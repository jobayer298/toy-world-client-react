import React from 'react';
import useTitle from '../../Hooks/useTitle';

const Blogs = () => {
    useTitle("Blogs")
    return (
        <div>
            <h2>blogs</h2>
        </div>
    );
};

export default Blogs;